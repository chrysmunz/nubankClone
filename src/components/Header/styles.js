import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        alignItems: 'center'
    },
    top: {
        flexDirection: 'row',
        marginTop: 20,
    },
    nome: {
        color: '#FFF',
        marginLeft: 5,
        fontSize: 22,
        fontWeight: 'bold'
    },
    footer: {
        marginTop: 8
    }
})

export default styles;